<!doctype html>
<html lang="en">
  <head>
    <!-- Required meta tags -->
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
    <!-- Custom CSS -->
    <link rel="stylesheet" href="{{ url_for('static', filename='style.css') }}">
    <title>Email Details</title>
  </head>
  <body>
    <div class="container">
        <!-- Fixed header with email subject -->
        <header class="email-header">
            <h1>Email: {{ email.subject }}</h1>
            <p><strong>Received Time:</strong> {{ email.received_time }}</p>
        </header>
        <!-- Scrollable content -->
        <div class="content">
            <div class="table-container">
                {% if tables %}
                <h2 class="text-center">Extracted Tables:</h2>
                {% for table in tables %}
                    <div class="table-responsive table-wrapper">
                        {{ table | safe }}
                    </div>
                {% else %}
                    <p class="text-center">No table data found.</p>
                {% endfor %}
                {% endif %}
            </div>
            <div class="text-center">
                <a href="/" class="btn btn-primary">Back to email list</a>
            </div>
        </div>
    </div>
  </body>
</html>
